TAILWIND_MODE=watch
MONGODB_URI=mongodb://localhost:27017?retryWrites=true&w=majority
MONGODB_DB=hacado
PUBLIC_DOMAIN=hacado.me
# IPv4 for custom-domain A records at apex/subdomain (optional). Shown in admin DNS help when set.
CUSTOM_DOMAIN_A_RECORD_IP=
SHORT_DOMAIN=haca.do
SMS_LINK_SHORTENING_ENABLED=false
ADMIN_DOMAIN=app.hacado.com
APPS_EXTERNAL_DOMAIN=apps.hacado.com
AUTH_PASSWORD=passwd
AUTH_SECRET="n6WKAtmUVdOYMRReNYXEd/sHLF/Ddfams+FAGMe7vzk=" 
SECRET_KEY="uw8iDFHFNjbIU8gPGMByTLiFfXJex7E1"
OUTLOOK_APP_CLIENT_ID=
OUTLOOK_APP_CLIENT_SECRET=
GOOGLE_CALENDAR_APP_CLIENT_ID=
GOOGLE_CALENDAR_APP_CLIENT_SECRET=
# Google OAuth for admin sign-in (can use same Google Cloud client values as calendar)
GOOGLE_AUTH_CLIENT_ID=
GOOGLE_AUTH_CLIENT_SECRET=
# Microsoft OAuth for admin sign-in (separate from OUTLOOK_APP_* calendar app)
MICROSOFT_AUTH_CLIENT_ID=
MICROSOFT_AUTH_CLIENT_SECRET=
MICROSOFT_AUTH_TENANT_ID=common
# Zoom OAuth for admin sign-in (separate from ZOOM_APP_* meeting app)
ZOOM_AUTH_CLIENT_ID=
ZOOM_AUTH_CLIENT_SECRET=
PAYPAL_ENVIRONMENT="sandbox"
# Square OAuth + Web Payments (register redirect {ADMIN_URL}/apps/oauth/square/redirect in Square Developer Console → OAuth)
SQUARE_APP_APPLICATION_ID=
SQUARE_APP_APPLICATION_SECRET=
# Application access token from the same Square application (Developer Dashboard → Credentials). Required for Webhook Subscriptions API (payment.updated); not the merchant OAuth token.
SQUARE_APP_ACCESS_TOKEN=
# Webhook signature key from the Square application (Developer Dashboard → Webhooks)
SQUARE_APP_WEBHOOK_SIGNATURE_KEY=
# sandbox | production (defaults to sandbox when NODE_ENV is not production)
SQUARE_ENVIRONMENT=sandbox
# Stripe Connect OAuth (register redirect {ADMIN_URL}/apps/oauth/stripe/redirect and Connect client id ca_… in Stripe Dashboard → Connect → settings)
STRIPE_CONNECT_CLIENT_ID=
# Platform secret key (sk_…); used for OAuth token exchange and API calls with Stripe-Account (Connect)
STRIPE_SECRET_KEY=
# Platform publishable key (pk_…); returned to the booking UI for Stripe.js (Payment Element)
STRIPE_PUBLISHABLE_KEY=
# Webhook signing secret for {APPS_EXTERNAL_URL}/api/webhooks/apps/name/stripe - subscribe to payment_intent.succeeded, payment_intent.payment_failed, charge.updated, charge.succeeded (fees), account.application.deauthorized
STRIPE_WEBHOOK_SECRET=
# Resend OAuth email sender app (one-time POST https://api.resend.com/oauth/register with client_secret_basic; redirect {ADMIN_URL}/apps/oauth/resend/redirect)
RESEND_OAUTH_CLIENT_ID=
RESEND_OAUTH_CLIENT_SECRET=
# Apple Pay: paste domain association from Stripe → Settings → Payment methods → Apple Pay (test vs live follows STRIPE_SECRET_KEY sk_test / sk_live)
# STRIPE_APPLE_PAY_DOMAIN_ASSOCIATION_TEST=
# STRIPE_APPLE_PAY_DOMAIN_ASSOCIATION_LIVE=
# STRIPE_APPLE_PAY_DOMAIN_ASSOCIATION=
TEXTBELT_API_KEY=
UNSPLASH_ACCESS_KEY=
PEXELS_API_KEY=
# When true, skip global member phone uniqueness (OTP still required)
ALLOW_DUPLICATE_MEMBER_PHONES=false
# BETTER_AUTH_TRUST_HOST=https://hacadodev.studio
# BETTER_AUTH_URL=https://hacadodev.studio

# Redis Configuration for BullMQ
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_DB=0

# S3 Configuration for assets
S3_REGION=us-east-1
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_ENDPOINT=
S3_BUCKET=
S3_FORCE_PATH_STYLE=

# BullMQ Queue Names
#BULLMQ_EMAIL_QUEUE_NAME=email-notifications
#BULLMQ_TEXT_MESSAGE_QUEUE_NAME=text-message-notifications

# Polar Subscriptions
# Absolute app URL for Polar checkout success/return redirects (defaults to request Host if unset)
# BETTER_AUTH_URL=https://your-admin-host
# ADMIN_URL=https://your-admin-host
# Create an Organization Access Token in your Polar dashboard settings (include customer_meters:read for SMS credit usage in admin)
POLAR_ACCESS_TOKEN=
# Signing secret from your Polar webhook endpoint configuration
POLAR_WEBHOOK_SECRET=
# Comma-separated slug:productId pairs (add matching benefit keys per slug in polar-billing.ts)
# Example: POLAR_BILLING_PLANS=free:prod_xxx,solo:prod_yyy,studio:prod_zzz
POLAR_BILLING_PLANS=
# "sandbox" for testing, "production" for live payments
POLAR_SERVER=sandbox
# Event name matching the SMS meter filter in Polar (usage ingest from TextBelt send/reply)
POLAR_SMS_USAGE_EVENT_NAME=
# Same meter id as the SMS credit benefit (used to gate built-in TextBelt sends via customer meters API)
POLAR_SMS_CREDITS_METER_ID=
# Optional: price line shown on the install Pro card (must match your Polar product; overrides install locale default)
NEXT_PUBLIC_PRO_SUBSCRIPTION_PRICE=

# Cloudflare Turnstile (admin signup + forgot-password). 
# Dummy always-pass keys for local:
# TURNSTILE_SITE_KEY=1x00000000000000000000AA
# TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Public signup country allowlist (NA/EU). Off unless set to "true".
SIGNUP_GEO_BLOCKING_ENABLED=false

# Reject disposable emails and RU/BY TLDs on public signup. On unless set to "false".
SIGNUP_EMAIL_BLOCKING_ENABLED=true